<p id="notice"><%= notice %></p>

<h1>Websites</h1>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Image Name</th>
      <th>Running</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @websites.each do |website| %>
      <tr>
        <td><%= website.title %></td>
        <td><%= website.image_name %></td>
        <td><%= website.running? ? "🔥" : "🛑"%></td>
        <td><%= link_to 'Show', website , class: 'btn btn-default'%></td>
        <td><%= link_to 'Edit', edit_website_path(website), class: 'btn btn-default' %></td>
        <td><%= link_to 'Destroy', website, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Website', new_website_path %>
